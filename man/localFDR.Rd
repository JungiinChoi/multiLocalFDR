% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/localFDR.R
\name{localFDR}
\alias{localFDR}
\title{Local False Discovery Rate (localFDR) Estimation}
\usage{
localFDR(
  z,
  tol = 5e-06,
  is_pvalue = FALSE,
  alternative = NULL,
  max_iter = 30,
  min_iter = 3,
  Uthre_gam = 0.99,
  Lthre_gam = 0.01,
  thre = 0.2
)
}
\arguments{
\item{z}{A numeric matrix or vector. Each row represents a data point (e.g., z-values or raw data).}

\item{tol}{Convergence threshold for the EM algorithm (default: 5e-6).}

\item{is_pvalue}{Logical; if `TRUE`, the input is treated as p-values and transformed (default: FALSE).}

\item{alternative}{Logical; if `TRUE`, assumes alternative distribution is greater than null (right-tailed); 
if `FALSE`, left-tailed; if `NULL`, determined automatically (default: NULL).}

\item{max_iter}{Maximum number of iterations for the EM algorithm (default: 30).}

\item{min_iter}{Minimum number of EM iterations before convergence is checked (default: 3).}

\item{Uthre_gam}{Upper threshold for gamma convergence (default: 0.99).}

\item{Lthre_gam}{Lower threshold for gamma convergence (default: 0.01).}

\item{thre}{Threshold for hypothesis rejection (used internally to determine the cutoff, default: 0.2).}
}
\value{
A numeric vector of estimated local FDR values for each observation.
}
\description{
`localFDR` estimates local false discovery rates using a semiparametric mixture model
via `SPMix()`, which combines Efron's empirical null principle with log-concave density estimation.
}
